server {
    listen 80;
    server_name claimflow.co.ke www.claimflow.co.ke;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Frontend (Next.js)
    location / {
        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # API (Fastify)
    location /api/ {
        proxy_pass http://api:8080/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        client_max_body_size 20M;
    }
}

# Uncomment the following block AFTER you have generated SSL certificates with Certbot
# server {
#     listen 443 ssl http2;
#     server_name claimflow.co.ke www.claimflow.co.ke;
#
#     ssl_certificate /etc/letsencrypt/live/claimflow.co.ke/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/claimflow.co.ke/privkey.pem;
#
#     location / {
#         proxy_pass http://web:3000;
#         ... same as above ...
#     }
#
#     location /api/ {
#         proxy_pass http://api:8080/;
#         ... same as above ...
#     }
# }
